import React from "react";
import ReactDOM from "react-dom/client";
import App from "@/interface-adapters/options/App";
import "@/interface-adapters/options/index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
